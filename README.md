# SIT725_220230157
In Week 4, the goal was to extend the Week 3 dynamic card system by introducing a backend REST API and connecting the application to a MongoDB database.
Instead of using hard-coded data, plant information is now stored in a database and fetched dynamically using Express and Mongoose.